<!DOCTYPE html>
<html>
  <head>
    <title>PrivateEventsTwo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav>


      <% if user_signed_in? %>
        You are logged in as: <%= link_to current_user.email, current_user %> | <%= link_to "Log Out", destroy_user_session_path, 
                                                                                data: { turbo_method: :delete } %> |
      <% else %>
        <%= link_to "Sign In", new_user_session_path %> |
      <% end %>
      <%= link_to "Home", events_path %> | <%= link_to "Create New Event", new_event_path %> |       
 
    </nav>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>
  </body>
</html>
