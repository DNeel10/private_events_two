<h1>Find an Event</h1>
<div>
  <h2>All Events</h2>

  <div>
    <% @events.each do |event| %>
      <%= link_to event.title, event %>
    <% end %>
  </div>

</div><br>

<%= link_to "Create new Event", new_event_path %>